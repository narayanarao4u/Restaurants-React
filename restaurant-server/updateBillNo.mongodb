use("restaurant");
db.orders.findOne();
// db.orders.updateMany({}, [{ $set: { billDate: "$updatedAt" } }]);
db.orders.aggregate([]);

/*
function updatebillNo() {
  let billNo = 0;
  let d = db.orders.find();

  d.forEach(function (r) {
    billNo += 1;
    db.orders.update({ _id: r._id }, { $set: { billNo: billNo } });
  });
}

db.orders.findOne({});

function testFunction() {
  let db = use("restaurant");
}
*/
